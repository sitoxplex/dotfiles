(defwidget clock []
    (box :class "clock" :orientation "h" :space-evenly false
        (label :class "time" :text {formattime(EWW_TIME, "%H:%M")})
        (label :class "month-date" :text {formattime(EWW_TIME, "%d %B")})
    )
)
